import { useEffect, useMemo, useState } from 'react';
import { ClassificationEditor } from './ClassificationEditor';
import { ProjectSupportEditor } from './ProjectSupportEditor';

type Activity={id:string;task_id:string;title:string;description:string;activity_type:string;required:number;documentation_field_count:number};
type Task={id:string;work_section_id:string;title:string;description:string;status:string};
type Section={id:string;work_area_id:string;name:string};
type Area={id:string;project_id:string;name:string};
type Project={id:string;name:string;property_designation?:string;status:string;work_area_count:number;work_section_count:number;task_count:number};
type Structure={areas:Area[];sections:Section[];tasks:Task[];activities:Activity[]};
type FieldMetadataItem={activity_id:string;context:'field'|'administrative';applicability?:string;surface?:string;lifecycle_stage?:string};
type Selection={kind:'project';id:string}|{kind:'area';id:string}|{kind:'section';id:string}|{kind:'task';id:string}|{kind:'activity';id:string};
const EMPTY_STRUCTURE:Structure={areas:[],sections:[],tasks:[],activities:[]};
const API_BASE='';

export function App(){
  const[projects,setProjects]=useState<Project[]>([]);const[projectId,setProjectId]=useState('');const[structure,setStructure]=useState<Structure>(EMPTY_STRUCTURE);const[selection,setSelection]=useState<Selection|null>(null);const[expanded,setExpanded]=useState<Set<string>>(new Set());const[state,setState]=useState<'loading'|'ready'|'error'>('loading');const[message,setMessage]=useState('');
  useEffect(()=>{void loadProjects(false)},[]);useEffect(()=>{if(projectId)void loadStructure()},[projectId]);
  async function loadProjects(preserve:boolean){try{const r=await fetch('/api/projects',{cache:'no-store'});if(!r.ok)throw new Error('Kunde inte hämta projekt.');const d=await r.json() as {projects:Project[]};setProjects(d.projects);if(!preserve){const first=d.projects[0]?.id||'';setProjectId(first);if(first)setSelection({kind:'project',id:first})}}catch(e){console.error(e);setState('error')}}
  async function loadStructure(selectAfter?:Selection){try{setState('loading');const[structureResponse,metadataResponse]=await Promise.all([fetch(`/api/studio/structure?projectId=${encodeURIComponent(projectId)}`,{cache:'no-store'}),fetch(`/api/project-field-metadata?projectId=${encodeURIComponent(projectId)}`,{cache:'no-store'})]);if(!structureResponse.ok)throw new Error('Kunde inte hämta projektstrukturen.');const next=await structureResponse.json() as Structure;if(metadataResponse.ok){const metadata=await metadataResponse.json() as {items?:FieldMetadataItem[]};const hidden=new Set((metadata.items||[]).filter(item=>item.applicability==='deprecated').map(item=>item.activity_id));next.activities=next.activities.filter(activity=>!hidden.has(activity.id));}setStructure(next);if(selectAfter){if(selectAfter.kind==='activity'&&!next.activities.some(activity=>activity.id===selectAfter.id))setSelection({kind:'project',id:projectId});else setSelection(selectAfter);}setState('ready')}catch(e){console.error(e);setState('error')}}
  async function api(path:string,method:'POST'|'PUT'|'DELETE',body?:Record<string,unknown>){setMessage(method==='DELETE'?'Tar bort…':'Sparar…');const r=await fetch(`${API_BASE}${path}`,{method,headers:body?{'Content-Type':'application/json'}:undefined,body:body?JSON.stringify(body):undefined});const d=await r.json().catch(()=>({})) as {ok?:boolean;id?:string;error?:string};if(!r.ok)throw new Error(d.error||'Åtgärden misslyckades.');setMessage(method==='DELETE'?'Borttaget':'Sparat');window.setTimeout(()=>setMessage(''),1600);return d}
  const tree=useMemo(()=>structure.areas.map(area=>({...area,sections:structure.sections.filter(s=>s.work_area_id===area.id).map(section=>({...section,tasks:structure.tasks.filter(t=>t.work_section_id===section.id).map(task=>({...task,activities:structure.activities.filter(a=>a.task_id===task.id)}))}))})),[structure]);
  const selectedObject=useMemo(()=>{if(!selection)return null;if(selection.kind==='project')return projects.find(x=>x.id===selection.id)||null;if(selection.kind==='area')return structure.areas.find(x=>x.id===selection.id)||null;if(selection.kind==='section')return structure.sections.find(x=>x.id===selection.id)||null;if(selection.kind==='task')return structure.tasks.find(x=>x.id===selection.id)||null;return structure.activities.find(x=>x.id===selection.id)||null},[selection,projects,structure]);
  const currentProject=projects.find(p=>p.id===projectId);
  const toggle=(key:string)=>setExpanded(c=>{const n=new Set(c);n.has(key)?n.delete(key):n.add(key);return n});
  async function createChild(){if(!selection||selection.kind==='activity')return;const labels={project:'arbetsområde',area:'arbetsavsnitt',section:'moment',task:'aktivitet'} as const;const name=window.prompt(`Namn på nytt ${labels[selection.kind]}:`)?.trim();if(!name)return;try{let result:{id?:string};let next:Selection;if(selection.kind==='project'){result=await api('/api/studio/work-areas','POST',{projectId,name});next={kind:'area',id:result.id!}}else if(selection.kind==='area'){result=await api('/api/studio/work-sections','POST',{workAreaId:selection.id,name});next={kind:'section',id:result.id!};setExpanded(c=>new Set(c).add(`area:${selection.id}`))}else if(selection.kind==='section'){result=await api('/api/studio/tasks','POST',{workSectionId:selection.id,title:name,description:''});next={kind:'task',id:result.id!};setExpanded(c=>new Set(c).add(`section:${selection.id}`))}else{result=await api('/api/studio/activities','POST',{taskId:selection.id,title:name,description:'',activityType:'work'});next={kind:'activity',id:result.id!};setExpanded(c=>new Set(c).add(`task:${selection.id}`))}await loadStructure(next);await loadProjects(true)}catch(e){setMessage(e instanceof Error?e.message:'Kunde inte skapa objektet.')}}
  async function saveSelection(form:{name:string;description:string;activityType:string}){if(!selection||selection.kind==='project')return;if(selection.kind==='area')await api(`/api/studio/work-areas/${selection.id}`,'PUT',{name:form.name});if(selection.kind==='section')await api(`/api/studio/work-sections/${selection.id}`,'PUT',{name:form.name});if(selection.kind==='task')await api(`/api/studio/tasks/${selection.id}`,'PUT',{title:form.name,description:form.description});if(selection.kind==='activity'){const activity=structure.activities.find(item=>item.id===selection.id);if(form.activityType==='project_condition'){if(!activity)return;if(!window.confirm(`Gör om ”${form.name}” till ett projektvillkor? Aktiviteten tas bort från arbetsflödet men bevaras som stående villkor.`))return;await api(`/api/studio/projects/${encodeURIComponent(projectId)}/project-conditions/from-activity/${encodeURIComponent(selection.id)}`,'POST');setMessage('Flyttad till Projektvillkor');await loadStructure({kind:'task',id:activity.task_id});return;}await api(`/api/studio/activities/${selection.id}`,'PUT',{title:form.name,description:form.description,activityType:form.activityType});}await loadStructure(selection)}
  async function deleteSelection(){if(!selection||selection.kind==='project')return;let path='';let parent:Selection;let name='';if(selection.kind==='area'){const x=structure.areas.find(i=>i.id===selection.id);if(!x)return;path=`/api/studio/work-areas/${x.id}`;parent={kind:'project',id:projectId};name=x.name}else if(selection.kind==='section'){const x=structure.sections.find(i=>i.id===selection.id);if(!x)return;path=`/api/studio/work-sections/${x.id}`;parent={kind:'area',id:x.work_area_id};name=x.name}else if(selection.kind==='task'){const x=structure.tasks.find(i=>i.id===selection.id);if(!x)return;path=`/api/studio/tasks/${x.id}`;parent={kind:'section',id:x.work_section_id};name=x.title}else{const x=structure.activities.find(i=>i.id===selection.id);if(!x)return;path=`/api/studio/activities/${x.id}`;parent={kind:'task',id:x.task_id};name=x.title}if(!window.confirm(`Ta bort ${selectionLabel(selection).toLowerCase()} ”${name}”?`))return;try{await api(path,'DELETE');await loadStructure(parent);await loadProjects(true)}catch(e){setMessage(e instanceof Error?e.message:'Kunde inte ta bort objektet.')}}
  return <div className="studio"><header className="topbar"><div className="brand"><span>BP</span><div><strong>ByggPlan Studio</strong><small>Projekteditor</small></div></div><select value={projectId} onChange={e=>{setProjectId(e.target.value);setSelection({kind:'project',id:e.target.value})}}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select>{message&&<div className="saveMessage">{message}</div>}<div className={`connection ${state}`}>{state==='ready'?'● Ansluten':state==='loading'?'Hämtar…':'API-fel'}</div></header><aside className="rail"><button className="active" title="Projekt">🌳<span>Projekt</span></button><button disabled title="Kontrollplan">📋<span>Kontrollplan</span></button><button disabled title="Dokument">📄<span>Dokument</span></button><button disabled title="Användare">👥<span>Användare</span></button></aside><aside className="treePanel"><div className="panelHeader"><div><small>PROJEKTSTRUKTUR</small><strong>{currentProject?.name||'Projekt'}</strong></div><button title="Skapa under markerat objekt" onClick={()=>void createChild()} disabled={!selection||selection.kind==='activity'}>＋</button></div><div className="tree">{state==='loading'&&<p className="muted">Hämtar struktur…</p>}{currentProject&&<TreeRow depth={0} icon="🏗" label={currentProject.name} selected={selection?.kind==='project'&&selection.id===currentProject.id} onSelect={()=>setSelection({kind:'project',id:currentProject.id})} badge={structure.areas.length.toString()}/>} {tree.map(area=>{const ak=`area:${area.id}`;return <div className="treeGroup" key={area.id}><TreeRow depth={1} icon="⛏" label={area.name} open={expanded.has(ak)} selected={selection?.kind==='area'&&selection.id===area.id} onToggle={()=>toggle(ak)} onSelect={()=>setSelection({kind:'area',id:area.id})}/>{expanded.has(ak)&&area.sections.map(section=>{const sk=`section:${section.id}`;return <div key={section.id}><TreeRow depth={2} icon="⌖" label={section.name} open={expanded.has(sk)} selected={selection?.kind==='section'&&selection.id===section.id} onToggle={()=>toggle(sk)} onSelect={()=>setSelection({kind:'section',id:section.id})}/>{expanded.has(sk)&&section.tasks.map(task=>{const tk=`task:${task.id}`;return <div key={task.id}><TreeRow depth={3} icon="▣" label={task.title} open={expanded.has(tk)} selected={selection?.kind==='task'&&selection.id===task.id} onToggle={()=>toggle(tk)} onSelect={()=>setSelection({kind:'task',id:task.id})} badge={task.activities.length.toString()}/>{expanded.has(tk)&&task.activities.map(activity=><TreeRow key={activity.id} depth={4} icon="○" label={activity.title} selected={selection?.kind==='activity'&&selection.id===activity.id} onSelect={()=>setSelection({kind:'activity',id:activity.id})}/>)}</div>})}</div>})}</div>})}</div></aside><main className="workspace"><div className="workspaceHeader"><div><small>INSPEKTÖR</small><h1>{selectionLabel(selection)}</h1></div><div className="headerActions"><button className="primary" onClick={()=>void createChild()} disabled={!selection||selection.kind==='activity'}>{createLabel(selection)}</button><button disabled>Förhandsgranska ↗</button></div></div><Inspector selection={selection} value={selectedObject} structure={structure} project={currentProject} onSave={saveSelection} onDelete={deleteSelection}/>{selection?.kind==='task'&&<ProjectSupportEditor ownerType="task" ownerId={selection.id}/>} {selection?.kind==='activity'&&<ProjectSupportEditor ownerType="activity" ownerId={selection.id}/>}</main></div>
}

function TreeRow({depth,icon,label,open,selected,badge,onToggle,onSelect}:{depth:number;icon:string;label:string;open?:boolean;selected:boolean;badge?:string;onToggle?:()=>void;onSelect:()=>void}){return <div className={`treeRow ${selected?'selected':''}`} style={{paddingLeft:10+depth*18}} onClick={onSelect}>{onToggle?<button onClick={e=>{e.stopPropagation();onToggle()}}>{open?'⌄':'›'}</button>:<span className="spacer"/>}<span className="nodeIcon">{icon}</span><span className="nodeLabel">{label}</span>{badge&&<small className="badge">{badge}</small>}</div>}
function Inspector({selection,value,structure,project,onSave,onDelete}:{selection:Selection|null;value:any;structure:Structure;project?:Project;onSave:(form:{name:string;description:string;activityType:string})=>Promise<void>;onDelete:()=>Promise<void>}){const[name,setName]=useState('');const[description,setDescription]=useState('');const[activityType,setActivityType]=useState('work');const[saving,setSaving]=useState(false);useEffect(()=>{setName(value?.title||value?.name||'');setDescription(value?.description||'');setActivityType(toEditorActivityType(value?.activity_type));setSaving(false)},[selection?.kind,selection?.id,value]);if(!selection||!value)return <EmptyInspector/>;if(selection.kind==='project')return <section className="overview"><div className="hero"><span>🏗</span><div><small>AKTIVT PROJEKT</small><h2>{project?.name}</h2><p>{project?.property_designation||'Ingen fastighetsbeteckning angiven'}</p></div></div><div className="stats"><Stat label="Arbetsområden" value={structure.areas.length}/><Stat label="Arbetsavsnitt" value={structure.sections.length}/><Stat label="Moment" value={structure.tasks.length}/><Stat label="Fältaktiviteter" value={structure.activities.length}/></div></section>;const childCount=selection.kind==='area'?structure.sections.filter(i=>i.work_area_id===selection.id).length:selection.kind==='section'?structure.tasks.filter(i=>i.work_section_id===selection.id).length:selection.kind==='task'?structure.activities.filter(i=>i.task_id===selection.id).length:value.documentation_field_count;async function save(){setSaving(true);try{await onSave({name,description,activityType})}finally{setSaving(false)}}return <section className="inspectorCard"><div className="objectType">{selectionLabel(selection)}</div><label><span>Namn</span><input value={name} onChange={e=>setName(e.target.value)}/></label>{(selection.kind==='task'||selection.kind==='activity')&&<label><span>Beskrivning / instruktion</span><textarea value={description} onChange={e=>setDescription(e.target.value)}/></label>}{selection.kind==='activity'&&<label><span>Aktivitetstyp</span><select value={activityType} onChange={e=>setActivityType(e.target.value)}><option value="work">Utför</option><option value="documentation">Dokumentera</option><option value="measurement">Mät</option><option value="control">Kontrollera</option><option value="approval">Godkänn</option><option value="wait">Anteckning / vänta</option><option value="project_condition">Projektvillkor</option></select>{activityType==='project_condition'&&<small>Aktiviteten flyttas ur arbetsflödet och blir ett stående villkor för projektet när du sparar.</small>}</label>}{selection.kind==='task'&&<div className="propertyRow"><span>Status</span><b>{value.status}</b></div>}<div className="propertyRow"><span>{selection.kind==='activity'?'Dokumentationsfält':'Underliggande objekt'}</span><b>{childCount||0}</b></div>{selection.kind==='activity'&&<ClassificationEditor activityId={selection.id}/>}<div className="formActions"><button className="danger" disabled={saving} onClick={()=>void onDelete()}>Ta bort</button><button className="primary" disabled={saving||!name.trim()} onClick={()=>void save()}>{saving?'Sparar…':'Spara ändringar'}</button></div></section>}
function toEditorActivityType(value:string|undefined){return ({perform:'work',document:'documentation',measurement:'measurement',check:'control',approval:'approval',note:'wait',choice:'wait'} as Record<string,string>)[value||'']||value||'work'}
function EmptyInspector(){return <div className="empty"><span>⌁</span><h2>Välj ett objekt i projektträdet</h2><p>Inspektören visar och redigerar arbetsområden, arbetsavsnitt, moment och fältaktiviteter.</p></div>}
function Stat({label,value}:{label:string;value:number}){return <article><strong>{value}</strong><span>{label}</span></article>}
function selectionLabel(selection:Selection|null){if(!selection)return 'Projektöversikt';return ({project:'Projektöversikt',area:'Arbetsområde',section:'Arbetsavsnitt',task:'Moment',activity:'Aktivitet'} as const)[selection.kind]}
function createLabel(selection:Selection|null){if(!selection||selection.kind==='activity')return 'Inget att skapa';return ({project:'+ Nytt arbetsområde',area:'+ Nytt arbetsavsnitt',section:'+ Nytt moment',task:'+ Ny aktivitet'} as const)[selection.kind]}
