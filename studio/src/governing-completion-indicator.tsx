import{useEffect}from'react';
export function GoverningCompletionIndicator(){useEffect(()=>{let tasks:any[]=[];let governed=new Set<string>();let timer=0;async function refresh(){const id=(document.querySelector('.projectWorkspace .topbar select')as HTMLSelectElement|null)?.value||'';if(!id)return;try{const[t,m]=await Promise.all([fetch(`/api/tasks?projectId=${encodeURIComponent(id)}&x=${Date.now()}`,{cache:'no-store'}),fetch(`/api/project-field-metadata?projectId=${encodeURIComponent(id)}&x=${Date.now()}`,{cache:'no-store'})]);tasks=t.ok?(await t.json()).tasks||[]:[];governed=new Set((m.ok?(await m.json()).items||[]:[]).filter((x:any)=>x.governing_documents?.length).map((x:any)=>x.activity_id));paint()}catch{}timer=window.setTimeout(refresh,1500)}function ok(a:string,s?:string,t?:string,x?:string){const list=tasks.flatMap((q:any)=>q.activities.filter((v:any)=>governed.has(v.id)&&q.workArea===a&&(!s||q.workSection===s)&&(!t||q.title===t)&&(!x||v.title===x)));return list.length>0&&list.every((v:any)=>v.done)}function mark(el:HTMLElement|undefined,done:boolean){const c=el&&Array.from(el.children).find(x=>x.classList.contains('hierGov'))as HTMLElement|undefined;if(c){c.classList.toggle('govComplete',done);c.classList.toggle('govPending',!done)}}function paint(){let a='',s='',t='';for(const r of Array.from(document.querySelectorAll('.projectTreeRow'))as HTMLElement[]){const d=Math.round((parseInt(r.style.paddingLeft||'10',10)-10)/16),l=(r.querySelector('.projectTreeLabel')?.textContent||'').trim();if(d===1)a=l;if(d===2)s=l;if(d===3)t=l;if(d===1)mark(r,ok(a));if(d===2)mark(r,ok(a,s));if(d===3)mark(r,ok(a,s,t));if(d===4)mark(r,ok(a,s,t,l))}}const changed=(e:Event)=>{const d=(e as CustomEvent<any>).detail;if(d?.activityId&&typeof d.done==='boolean'){for(const q of tasks)for(const a of q.activities)if(a.id===d.activityId)a.done=d.done;paint()}};window.addEventListener('byggplan:activity-status-changed',changed);void refresh();return()=>{clearTimeout(timer);window.removeEventListener('byggplan:activity-status-changed',changed)}},[]);return null}
